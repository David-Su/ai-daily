services:
  ai-daily:
    image: ai-daily
    build: .
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./resources:/app/resources
      - ./news-data:/app/news-data
